{% block block_mw_text_video %}
    {% set element = block.slots.getSlot('textVideo') %}

    {% if element %}
        <div class="cms-block-mw-text-video">
            <div data-cms-element-id="{{ element.id }}">
                {% sw_include '@Storefront/storefront/element/cms-element-' ~ element.type ~ '.html.twig'
                    ignore missing
                    with { element: element }
                %}
            </div>
        </div>
    {% endif %}
{% endblock %}
