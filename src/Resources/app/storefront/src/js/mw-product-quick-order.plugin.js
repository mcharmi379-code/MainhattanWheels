import Plugin from 'src/plugin-system/plugin.class';

export default class MwProductQuickOrderPlugin extends Plugin {
    init() {
        // No interactive quantity controls — plugin kept for future extensibility
    }
}
